# Port facts — <port name + version> (probed YYYY-MM-DD)

| # | Question | Probe ran | Evidence (verbatim output) | Conclusion |
|---|---|---|---|---|
| P1 | agent home + project instruction file | ... | ... | e.g. `.acme/` + `ACME.md`; set `git config serpens.agentDir .acme` |
... (P2–P8)
Re-probe on every port or OpenSpec upgrade before rollout. See `docs/SETUP.md` stage 2.
