A planted fixture proving the shape gate accepts a command that reads the facts instead.
Read `docs/testing-stack.md` and follow its `Manual testing access` section.
Give every part `http-request-idiom` requires, then once more in the form `http-client` takes.
Name the destination as `event-addressing` names it and the body as `event-payload-format` gives.
Seed the way `store-seed-idiom` sanctions; read it back in the `store-query-idiom` form.
On rejection, name what `error-routing` names, and read anything else via `observation-access`.
