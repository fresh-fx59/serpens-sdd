A planted fixture for the shape gate's own negative control. It names no product at all —
which is exactly the point: the noun gate above passes it, and it still describes the stack.
HTTP requests go as one `curl` line, so give the method and the full path.
Print the topic, the message key, and the complete JSON event the tester sends.
Preconditions are runnable `INSERT` statements; the expected result is the `SELECT` to run.
On rejection, name the dead-letter topic and what lands there. Give the HTTP status.
