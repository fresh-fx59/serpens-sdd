A planted fixture proving the gate does not flag the kit's own prerequisites.
Requires git >= 2.13, node >= 18, and lefthook installed through the approved channel.
The OpenSpec CLI is pinned; `index-code` additionally needs Universal Ctags and Zoekt.
Corpus size and corporate policy are ordinary words and must not trip anything.
